
# Drag and Drop Test App

Цей проект є демонстраційним додатком для перевірки та показу можливостей фреймворку **[DragDropFramework](../)**.

Додаток реалізує функціонал домашньої бібліотеки, де користувач може шукати книги через Google Books API, додавати їх до своєї колекції та організовувати процес читання за допомогою Kanban-дошки.

## 📱 Функціонал

- **Пошук книг:** Інтеграція з Google Books API для пошуку та додавання книг.
- **Локальне сховище:** Всі дані зберігаються в CoreData.
- **Kanban Board (Трекер):** Перетягування книг між статусами "Читаю зараз" та "Прочитано" (демонстрація Cross-List Drag & Drop).
- **Сортування:** Зміна пріоритету книг у списку перетягуванням (демонстрація Single List Reorder).
- **Деталі:** Перегляд інформації та зміна прогресу читання через слайдер.

## 🛠 Технічний стек

- **SwiftUI** (MVVM Архітектура)
- **CoreData** (Збереження стану та порядку елементів)
- **DragDropFramework** (Власне рішення для Drag & Drop)

## Project Structure

```
RawDragAndDropProj/
├── Views/                # SwiftUI views
├── Models/               # Data models
├── ViewModels/           # Business logic
└── Services/             # Utilities
```

## 📦 Інтеграція Фреймворку

Цей додаток демонструє, наскільки просто реалізувати складні UI-патерни за допомогою `DragDropFramework`.

### 1. Реалізація Kanban (Трекер)
Замість написання сотень рядків коду з `DropDelegate`, ми використовуємо один модифікатор `.easyCrossListReorder`:

```swift
// ShelfSectionView.swift

ForEach(books) { book in
    ShelfRow(book: book)
        .easyCrossListReorder(
            item: book,
            items: $books,
            draggedItem: $draggedBook,
            listId: listId, // "reading" або "finished"
            onDataChanged: { viewModel.saveReorderedList() },
            onMoveBetweenLists: { item, targetId in
                // Вся бізнес-логіка залишається у ViewModel
                viewModel.moveBook(book: item, toTargetListID: targetId)
            }
        )
}
